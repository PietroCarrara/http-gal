module.exports = class BookRepository {

    /**
     * Get a book by its name
     *
     * @param {string} name The book name
     * @return {Promise<Book>}
     */
    getByName(name) {
        
    }
}